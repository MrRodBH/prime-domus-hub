# Tenant login browser identity — 2026-09-14

Owner reported that tenant login inherited the Real One favicon. Reused AGENTS.md, OWNER_UI_APPROVALS.md, BROWSER_IDENTITY_CORRECTION.md and the applied CMS acceptance; base is PR288 documentation head 7b90e5453b627798fa7cd0ef765cb1cee483a3c8 (deployed application source162f014d59c1f1595c2915338ab701b70190c530). Preserve all later CMS behavior and both hosting providers.

Cause: tenant-independent root loading deliberately skips public CMS/tracking on authentication routes, but its platform favicon leaked into /$tenantSlug/auth. That child also emitted a fixed RM Prime SaaS title and AuthPage rendered RM Prime's logo for every tenant.

Smallest correction: keep the root loading/auth boundary intact, omit its platform favicon specifically for contextual tenant login, and give the child a public presentation-only loader. The existing server domain authority must resolve a tenant whose verified slug equals the route slug before reading its published settings. Return only name/logo/favicon. The URL slug cannot select a tenant; unknown domains, mismatches and unavailable branding receive a neutral company mark and retain the existing login flow. Published favicon retains precedence; the original RM Prime fallback remains restricted to its verified UUID. The page logo now follows the same verified branding instead of hardcoding RM Prime for every company. No credentials, membership rules, database, CMS snapshots, cookies, DNS or domain-state changes.

Tenant title: company name followed by “Acesso da empresa”. Platform /auth retains “Acesso à plataforma — REAL ONE” and R1 favicon. Typecheck and brand-identity tests include three companies, published-media priority, absent/mismatched identity and neutral fallback. Production build and live deployment evidence are recorded in the PR after application; source alone does not establish live acceptance.

Apply only five source files: src/lib/brand-assets.ts, src/lib/api/site.functions.ts, src/routes/$tenantSlug.auth.tsx, src/routes/__root.tsx, src/components/auth/AuthPage.tsx. Preserve the four canonical build-only files in the existing isolated Cloudflare build and all eight runtime bindings. Do not perform GitHub operations through Lovable. Owner acceptance: compare /rmprime/auth on rmprimeimoveis.com.br against /auth on realone.com.br; tenant must have its own name/favicon and platform must retain Real One.
