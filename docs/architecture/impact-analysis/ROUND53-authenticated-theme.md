# Round 53 — authenticated entry and approved presentation

Base: main@7996eb9a18b6478477fb8b29f32051c6ec829b8b. User reports the canonical SaaS root opening the demonstration and the persistent onboarding appearing inside the legacy workspace.

Change: exact realone/www SaaS roots enter existing /auth; tenant website roots and explicit demo routes remain intact. The existing single authenticated shell receives the approved petroleum rail, warm surfaces, fonts and rounded controls. Super Admin begins with Dashboard and separate plans/tenant views backed by loadSuperOnboarding. Editing opens the stored company directly; successful writes return to Dashboard. Search is explicit and client presentation only; all existing database tenants remain available. Legacy lifecycle/member/impersonation functions remain accessible under an operational disclosure.

No APIs, credentials, migrations, database records, server authorization or permission changes. No demo storage imported. Twelve-Factor: no new environment binding, state or deployment coupling. Scope guards are extended with an exact successor path list; previous behavior assertions continue except the explicitly superseded SaaS root. The new test checks the approved 272px rail; the historical FVS1 release-width specimen remains intact.

Verification: Round52 controlled DOM is evolved for navigation, save-return, fresh-cache remount, search without deleting records, edit/failure preservation. Round53 tests execute the actual entry helper and check theme/authorization boundaries. Existing Round51 login/logout and Round52 native SQL/controlled server gates remain applicable. An optional standalone historical FVS1 run rejected its old full-diff allowlist (.env changed before this round); it is not an applicable release gate and was not relaxed. Browser pixel comparison and real logged-in owner acceptance remain separate; controlled fixtures do not prove deployed persistence or a 30-day retention period.

Preserved: #229 support pending; #231 unmerged/undeployed; #227/#230; Round45 browser backlog; LSR-02 Rejected — Terminal, budget 0/2.
