# GA-08.1.2 — Documentation Roadmap Namespace & Phase Identity Correction

## Status

Ready for External Audit

## 1. Objetivo

Corrigir a taxonomia da estrutura documental criada pela GA-08.1,
preservando explicitamente a identidade histórica da
`Product Roadmap · Fase 6 — Product UX Refactor (Experiência do Produto)`
e separando os dois roadmaps que coexistem no projeto: Product Roadmap e
Architectural Roadmap. A etapa também finaliza os dois bloqueios
documentais remanescentes da GA-08.1.1 (colisão de tokens de Status no
Document Repository Contract e evidência Git desatualizada).

Etapa exclusivamente de governança e reorganização documental. Nenhum
código funcional, runtime, migration, schema, RLS, grant, provider,
frontend ou etapa da cadeia SCP-012 é iniciada, alterada ou desbloqueada.

## 2. Contexto histórico

`Product Roadmap · Fase 6 — Product UX Refactor (Experiência do Produto)`
foi uma fase real e formal da evolução do RM Prime OS. Foi nessa fase
que o produto passou a ser direcionado estruturalmente para uma
plataforma SaaS, consolidando Workspace First, EntityWorkspace,
Resolution Graph, Plugin Marketplace, feature flags e os fundamentos da
experiência integrada do produto.

Registrar essa fase apenas como diretório `product-ux-refactor/` sem o
número `06` constitui perda de rastreabilidade histórica e foi corrigido
por esta etapa.

## 3. Problema de namespace confirmado

Antes desta etapa, `docs/delivery/**` continha quatro diretórios de
fase misturando duas taxonomias distintas:

```text
docs/delivery/product-ux-refactor/               (Product Roadmap · Fase 6)
docs/delivery/phase-02-multi-tenancy/            (Architectural Roadmap · Fase 2)
docs/delivery/phase-03-membership-evolution/     (Architectural Roadmap · Fase 3)
docs/delivery/phase-04-saas-commercial-platform/ (Architectural Roadmap · Fase 4)
```

Essa coexistência sugeria uma sequência numérica global falsa e
apagava o número histórico `06`.

## 4. Gate corretivo da GA-08.1.1

### 4.1 Colisão de tokens de Status

A §8.1 do `docs/governance/DOCUMENT_REPOSITORY_CONTRACT.md` deixou de
listar os tokens canônicos em linhas isoladas dentro de bloco de código
e passou a apresentá-los em tabela, de modo que somente o Status real
do documento corresponde às regexes canônicas.

Validação real (`grep`):

```text
^## Status$                = 1
^Ready for External Audit$ = 1
^Accepted$                 = 0
```

### 4.2 Evidência Git real da GA-08.1.1

Estado Git inspecionado antes desta etapa:

```text
BRANCH        = edit/edt-e39b6b6b-f1b2-4f5d-b633-68c6d868b219
HEAD BEFORE   = c4dcc273b8d84fc56b77f0d0fa5770284e169d3a
LOG (top-5)   = c4dcc27 Finalizou GA-08.1.1 auditoria
                b241e4f Changes
                afbb5a7 Changes
                81ee4f9 Changes
                54a4ffb Executado GA-08.1 migr. docs
REMOTES       = origin (git.private.lovable-gcp), secondary (s3)
```

Mapeamento por etapa:

- **GA-08.1 — materialização:** commit `54a4ffb` ("Executado GA-08.1
  migr. docs").
- **GA-08.1.1 — commits intermediários:** `81ee4f9`, `afbb5a7`,
  `b241e4f` (todos rotulados "Changes" pelo harness).
- **GA-08.1.1 — commit final persistido:** `c4dcc27` ("Finalizou
  GA-08.1.1 auditoria").
- **HEAD real imediatamente antes da GA-08.1.2:** `c4dcc27`.

O valor `b241e4f`, mencionado na versão anterior da evidência, é
intermediário e não representa o HEAD final da GA-08.1.1.

## 5. Taxonomia de roadmaps

O projeto possui dois roadmaps distintos, agora formalizados como
namespaces separados sob `docs/delivery/`:

- **Product Roadmap** — `docs/delivery/product-roadmap/`. Sequência
  histórica de evolução geral do produto. Autoridade: histórico
  oficial do produto RM Prime OS.
- **Architectural Roadmap** — `docs/delivery/architectural-roadmap/`.
  Sequência posterior governada por Architecture First e por
  `docs/architecture/ROADMAP_ARCHITECTURAL.md`.

**Regra de não comparação numérica global:** os números de fase dos
dois roadmaps não formam uma sequência global única. Toda referência
futura a uma fase deve declarar seu namespace, na forma:

```text
Product Roadmap · Fase 6 — Product UX Refactor (Experiência do Produto)
Architectural Roadmap · Fase 4 — SaaS Commercial Platform
```

## 6. Estrutura anterior

Registrada como mapa histórico explícito (referência única permitida a
estes caminhos, além do próprio contrato quando descreve o mesmo mapa):

```text
docs/delivery/product-ux-refactor/
docs/delivery/phase-02-multi-tenancy/
docs/delivery/phase-03-membership-evolution/
docs/delivery/phase-04-saas-commercial-platform/
```

## 7. Estrutura final

```text
docs/
├── architecture/
├── delivery/
│   ├── product-roadmap/
│   │   └── phase-06-product-ux-refactor/
│   └── architectural-roadmap/
│       ├── phase-02-multi-tenancy/
│       ├── phase-03-membership-evolution/
│       └── phase-04-saas-commercial-platform/
└── governance/
```

## 8. Mapeamento

Aplicados exatamente os quatro renames de diretório:

```text
docs/delivery/product-ux-refactor/
  -> docs/delivery/product-roadmap/phase-06-product-ux-refactor/

docs/delivery/phase-02-multi-tenancy/
  -> docs/delivery/architectural-roadmap/phase-02-multi-tenancy/

docs/delivery/phase-03-membership-evolution/
  -> docs/delivery/architectural-roadmap/phase-03-membership-evolution/

docs/delivery/phase-04-saas-commercial-platform/
  -> docs/delivery/architectural-roadmap/phase-04-saas-commercial-platform/
```

Os 128 arquivos preservaram nome, conteúdo e conjunto. Nenhum arquivo
foi reclassificado individualmente. Nenhum nome de arquivo foi alterado.

## 9. Integridade SHA-256

Antes da movimentação, manifesto `/tmp/ga08-1-2-before.sha256`
capturou 128 entradas sob os quatro diretórios antigos. Após a
movimentação, manifesto `/tmp/ga08-1-2-after.sha256` foi gerado sob os
novos caminhos e comparado por hash+basename (namespace de diretório
descartado):

```text
diff /tmp/before.txt /tmp/after.txt = (vazio)
128 de 128 hashes idênticos
```

Nenhum manifesto temporário foi persistido no repositório.

## 10. Método de movimentação

`git mv` bloqueado pelo harness (política do sandbox); a movimentação
foi feita com `mv` recursivo do diretório, seguido de validação por
SHA-256 (§9) e substituição das referências ativas por mapa exato
(§8). O Git reconhecerá os renames por similaridade de conteúdo. Não
houve cópia+exclusão, recriação manual nem edição de conteúdo durante
a movimentação.

## 11. Referências atualizadas

Substituição literal dos quatro caminhos exatos (§8) em toda a árvore,
excluindo `node_modules/**` e `.git/**`. Após a substituição:

```text
rg 'docs/delivery/product-ux-refactor
   |docs/delivery/phase-02-multi-tenancy
   |docs/delivery/phase-03-membership-evolution
   |docs/delivery/phase-04-saas-commercial-platform' \
   . --glob '!node_modules/**' --glob '!.git/**'
=> (permitido apenas neste documento como mapa histórico)
```

Nenhuma referência ativa aponta para os caminhos anteriores.
Nenhuma substituição genérica de `docs/delivery/` foi aplicada.

## 12. Identidade da Fase 6

Nome oficial preservado em `ROADMAP_ARCHITECTURAL.md`, no Document
Repository Contract e neste documento:

```text
Product Roadmap · Fase 6 — Product UX Refactor (Experiência do Produto)
```

Descrição oficial:

> Fase histórica em que o RM Prime OS foi direcionado para uma
> plataforma SaaS e em que foram consolidados os fundamentos de
> experiência integrada do produto (Workspace First, EntityWorkspace,
> Resolution Graph, Plugin Marketplace, feature flags).

## 13. Document Repository Contract

Atualizações aplicadas em `docs/governance/DOCUMENT_REPOSITORY_CONTRACT.md`:

- §3 — Estrutura canônica reescrita com os novos namespaces.
- §3.1 — Nova subseção "Roadmap namespaces" definindo Product Roadmap
  e Architectural Roadmap, sua autoridade, a regra de não comparação
  numérica global e a identidade oficial de cada fase, incluindo a
  Fase 6.
- §5 — Regra de `docs/delivery/` atualizada para os diretórios
  canônicos sob os dois namespaces.
- §8.1 — Listagem de status permitidos convertida em tabela, para
  eliminar a colisão de tokens canônicos.

## 14. Roadmap

Seção `6.2 GA-08` do `docs/architecture/ROADMAP_ARCHITECTURAL.md`
atualizada para:

```text
GA-08     — In Progress.
GA-08.0   — Accepted.
GA-08.1   — Accepted.
GA-08.1.1 — Accepted.
GA-08.1.2 — Ready for External Audit.
GA-08.2   — futura; não iniciada.
```

Nenhuma entrada da cadeia SCP-012, SCP-012.0, SCP-012.0.1 ou
SCP-012.0.1.1 foi alterada por esta etapa.

## 15. Validação de links

Validador canônico read-only aplicado a `docs/**/*.md`, removendo
fenced code blocks e inline code, ignorando `http://`, `https://`,
`mailto:`, `data:` e âncoras internas, e resolvendo cada link relativo
a partir do arquivo de origem. Saída real:

```text
All relative Markdown links under docs/** resolve.
```

## 16. Evidência Git

Estado Git registrado ao final desta etapa (após harness persistir os
arquivos alterados):

```text
BRANCH = edit/edt-e39b6b6b-f1b2-4f5d-b633-68c6d868b219
HEAD   = 4adc6af55964099a90e8586c71ba81be03249007
LOG    = 4adc6af Changes
         f2ee804 Changes
         6ae5cab Changes
         686a63d Changes
         31864c4 Changes
         66a86b0 Changes
         e92f7cc Changes
         953b168 Changes
STATUS = working tree limpo
```

Nenhum `push --force`, `reset`, `rebase`, `amend` ou mudança manual de
branch foi executado. O HEAD `4adc6af` é o commit final real da
GA-08.1.2 no momento do registro. Commits adicionais eventualmente
gerados pelo harness após este registro serão HEAD reais posteriores e
serão anotados na próxima etapa que consultar esta cadeia.

## 17. Arquivos movidos e alterados

Movidos (4 renames de diretório, 128 arquivos preservados):

```text
docs/delivery/product-ux-refactor
  -> docs/delivery/product-roadmap/phase-06-product-ux-refactor
docs/delivery/phase-02-multi-tenancy
  -> docs/delivery/architectural-roadmap/phase-02-multi-tenancy
docs/delivery/phase-03-membership-evolution
  -> docs/delivery/architectural-roadmap/phase-03-membership-evolution
docs/delivery/phase-04-saas-commercial-platform
  -> docs/delivery/architectural-roadmap/phase-04-saas-commercial-platform
```

Alterados (referências ativas atualizadas por mapa exato):

```text
docs/architecture/ROADMAP_ARCHITECTURAL.md
docs/architecture/impact-analysis/GA-08.0-...md
docs/architecture/impact-analysis/GA-08.1-...md   (Status -> Accepted)
docs/architecture/impact-analysis/GA-08.1.1-...md (Status -> Accepted)
docs/architecture/impact-analysis/ (demais arquivos com referências)
docs/architecture/ADR/*.md
docs/architecture/security/*.md
docs/architecture/impact-analysis/README.md
docs/governance/DOCUMENT_REPOSITORY_CONTRACT.md
```

Criado:

```text
docs/architecture/impact-analysis/GA-08.1.2-documentation-roadmap-namespace-phase-identity-correction.md
```

Alterações comment-only autorizadas em `src/**` (apenas referências
documentais em comentários; nenhum token executável, AST, tipo ou
comportamento afetado):

```text
src/integrations/supabase/impersonation-state.ts
src/lib/storage/signed-url.ts
```

## 18. Confirmações negativas

Confirmo expressamente:

```text
nenhum arquivo histórico foi perdido;
nenhum relatório foi renomeado;
nenhum conteúdo histórico foi reclassificado incorretamente;
nenhum código executável foi alterado;
nenhum teste foi alterado;
nenhuma migration foi alterada;
nenhum schema foi alterado;
nenhuma RLS policy foi alterada;
nenhum grant foi alterado;
nenhum provider foi alterado;
nenhum status da cadeia SCP-012 foi alterado;
GA-08.2 não foi iniciada;
SCP-012.0.2 não foi iniciada;
SCP-012 permanece bloqueada.
```
