# SCP-012.0 — Transaction-Safe Commercial Authority & Membership Mutation Boundary Impact Analysis

## Status

Accepted

## 1. Objetivo

Executar a análise arquitetural completa dos dois pré-requisitos que impedem
a implementação da SCP-012 (Commercial Seat Limit Atomic Enforcement
Integration):

1. ausência de autoridade comercial executável dentro de uma transação
   Postgres — hoje a decisão comercial existe exclusivamente em TypeScript
   (`src/lib/api/commercial/**`), fora do plano transacional do banco;
2. ausência de um boundary server-side de mutations de membership — hoje
   não existe qualquer server function, RPC, repository, hook ou fluxo de
   invitation/aceite que altere `public.tenant_members` em runtime.

Esta etapa é exclusivamente de planejamento, documentação e confirmação de
estado. Nenhuma migration, RPC, mutation ou enforcement é criado.

## 2. Estado atual confirmado

- SCP-011, SCP-011.1, SCP-011.2, SCP-011.3, SCP-011.3.1, SCP-011.3.2,
  SCP-011.3.3 = **Accepted**.
- SCP-012 = **Blocked — Architectural Prerequisite Required**.
- Nenhuma implementação parcial da SCP-012 existe (nenhum resolver SQL,
  nenhuma migration, nenhuma mutation, nenhum lock, nenhum boundary).
- A autoridade comercial canônica existe somente em TypeScript e é
  read-only, conforme aprovado na cadeia SCP-011.
- Não existem mutations runtime de `public.tenant_members`.

## 3. Inventário de mutations

### 3.1 Comandos executados

```
rg -n 'tenant_members' src/ supabase/
rg -n '\.(insert|update|upsert|delete)\(' src/
rg -n '\.rpc\(' src/ supabase/
rg -n 'membership_status' src/ supabase/
rg -ni 'invite|invitation' src/
rg -n 'service_role|serviceRole|createClient' src/
```

### 3.2 Referências a `tenant_members` (runtime)

Todas leituras — nenhuma escrita:

| Arquivo | Linha(s) | Natureza | Filtro |
|---|---|---|---|
| `src/lib/api/tenant-selection.functions.ts` | 35–38 | SELECT | `membership_status = 'active'` |
| `src/lib/api/super.functions.ts` | 93, 122 | SELECT COUNT | agregações super-admin |
| `src/lib/api/commercial/seat-usage-reader.ts` | 54–57 | SELECT COUNT | `membership_status IN ('active','invited')` |
| `src/integrations/supabase/tenant-repository.ts` | 61, 79 | SELECT | resolução de tenant |
| `src/integrations/supabase/tenant-middleware.ts` | — | via RPC `is_super_admin` / `get_current_tenant_id` | leitura indireta |

### 3.3 Mutations `.insert / .update / .upsert / .delete` em `tenant_members`

Nenhuma. A única inserção existente é o seed no migration
`20260701204508_...sql` (linhas 135–139), executado com privilégios de
migração — não é runtime.

### 3.4 RPCs

Nenhuma RPC muta `tenant_members`. As RPCs relacionadas
(`is_super_admin`, `get_current_tenant_id`, `user_belongs_to_tenant`,
`user_has_active_membership`, `has_role`, `has_permission`,
`has_cms_permission`) são todas **stable / read-only**.

### 3.5 Convites / aceite / suspensão / revogação / reativação

Nenhum fluxo existe. Buscas por `invite`, `invitation` retornam apenas
referências a e-mail template (`invite.tsx`) e ao endpoint de bootstrap
inicial, não a mutations de membership.

### 3.6 Confirmações negativas explícitas

- não existe mutation direta de `tenant_members`;
- não existe mutation via RPC;
- não existe mutation via repository abstrato;
- não existe fluxo de invitation;
- não existe aceite de convite;
- não existe reativação ou suspensão server-side;
- não existe mutation client-side autorizada.

## 4. Schema, RLS e grants (DDL real)

### 4.1 Tabela `public.tenant_members`

Coalesced a partir de `20260701204508` + `20260708125042`:

```sql
CREATE TABLE public.tenant_members (
  tenant_id          uuid    NOT NULL REFERENCES public.tenants(id) ON DELETE CASCADE,
  user_id            uuid    NOT NULL REFERENCES auth.users(id)      ON DELETE CASCADE,
  is_owner           boolean NOT NULL DEFAULT false,
  is_default         boolean NOT NULL DEFAULT true,
  joined_at          timestamptz NOT NULL DEFAULT now(),
  tenant_role        public.tenant_role       NOT NULL DEFAULT 'viewer',
  membership_status  public.membership_status NOT NULL DEFAULT 'active',
  invited_at         timestamptz,
  accepted_at        timestamptz,
  suspended_at       timestamptz,
  revoked_at         timestamptz,
  updated_at         timestamptz NOT NULL DEFAULT now(),
  PRIMARY KEY (tenant_id, user_id)
);

CREATE INDEX tenant_members_user_idx           ON public.tenant_members(user_id);
CREATE INDEX tenant_members_tenant_idx         ON public.tenant_members(tenant_id);
CREATE INDEX tenant_members_active_lookup_idx  ON public.tenant_members(user_id, tenant_id, membership_status);

CREATE TRIGGER tg_tenant_members_set_updated_at
  BEFORE UPDATE ON public.tenant_members
  FOR EACH ROW EXECUTE FUNCTION public.tg_set_updated_at();
```

Enums:

```sql
CREATE TYPE public.tenant_role
  AS ENUM ('owner','admin','manager','broker','captador','secretaria','viewer');

CREATE TYPE public.membership_status
  AS ENUM ('active','invited','suspended','revoked');
```

### 4.2 Grants

```sql
GRANT SELECT ON public.tenant_members TO authenticated;
GRANT ALL    ON public.tenant_members TO service_role;
-- (sem GRANT para anon)
```

### 4.3 RLS

```sql
ALTER TABLE public.tenant_members ENABLE ROW LEVEL SECURITY;

-- SELECT
CREATE POLICY tm_select ON public.tenant_members
  FOR SELECT TO authenticated
  USING (
    public.is_super_admin()
    OR user_id = auth.uid()
    OR public.user_belongs_to_tenant(tenant_id)
  );

-- FOR ALL (permissive, default)
CREATE POLICY tm_write ON public.tenant_members
  FOR ALL TO authenticated
  USING      (public.is_super_admin())
  WITH CHECK (public.is_super_admin());
```

Ambas as policies são **PERMISSIVE** (default do Postgres — `CREATE POLICY`
sem cláusula `AS RESTRICTIVE`).

### 4.4 Efeito real (policy + grants)

| Role            | SQL privilege efetivo em `tenant_members`             | Passa por RLS? |
|-----------------|--------------------------------------------------------|----------------|
| `anon`          | nenhum (sem GRANT)                                     | irrelevante — negado antes da RLS |
| `authenticated` | apenas `SELECT` (GRANT restrito a SELECT)              | sim — `tm_select` |
| `authenticated` | `INSERT/UPDATE/DELETE` — **não concedido**             | RLS `tm_write` seria avaliada, mas nunca alcança porque o GRANT bloqueia primeiro |
| `service_role`  | `ALL` (GRANT ALL); RLS **bypassada** por definição do service role | — |

Consequência arquitetural crítica: **hoje, um usuário `authenticated` não
consegue mutar `tenant_members` diretamente** por falta de GRANT, mesmo com
a policy `tm_write`. Qualquer futura RPC `SECURITY DEFINER` roda como owner
e ignora essa proteção — portanto o boundary de segurança da SCP-012 não
pode depender de `tm_write` isolada; precisa depender do desenho da RPC /
server fn (GRANT EXECUTE restrito + verificação explícita de autorização).

## 5. Autoridade comercial atual

A decisão comercial existe hoje inteiramente em TypeScript, server-side,
read-only, atrás de `requireTenant`:

| Módulo | Responsabilidade |
|---|---|
| `feature-catalog.ts` | catálogo canônico de feature keys + `evaluateFeatureCatalogGate` (short-circuit `not_evaluated`) |
| `feature-gate.ts` | `decideCommercialFeature` (entitled / not_entitled / billing_* / not_evaluated), reasons e source enum |
| `read-models.ts` | `deriveCommercialSummary`, `deriveEntitlementSnapshot`, `deriveBillingHealth`, projeções DTO whitelisted |
| `limit-decision.ts` | `normalizeCommercialSeatLimitInput` (strict input), tipos `CommercialLimitDecision` |
| `seat-limit-runtime.ts` | orquestração: catalog gate → feature decision → extração de limite → COUNT via reader |
| `seat-usage-reader.ts` | única leitura autoritativa `tenant_members COUNT` (`membership_status IN ('active','invited')`) |
| `commercial.functions.ts` | server functions (`getCommercialFeatureDecision`, `getCommercialSeatLimitDecision`, etc.) — loader único de contexto (`loadTenantCommercialContext`) |

Tabelas consultadas (todas via `supabaseAdmin`, exclusivamente SELECT):

- `tenant_subscriptions` — status/priority, `plan_id`;
- `commercial_plans` — nome/status do plano ativo;
- `commercial_plan_entitlements` — valores por plano;
- `tenant_entitlements` — overrides por tenant, com precedência;
- `tenant_billing_provider_mappings` — provider ativo;
- `billing_events` — último evento (apenas `received_at`, `processing_status`);
- `tenant_members` — COUNT com filtro `membership_status IN ('active','invited')`.

Precedência de entitlement (SCP-004+): `tenant_entitlements` > plan
entitlements > default (`source ∈ { 'tenant','plan','default','none' }`).

Extração de `users.seats`: dentro de `seat-limit-runtime.ts`, extraído do
`TenantEntitlementSnapshot`. Se o valor estiver ausente / inválido / não
efetivo, o short-circuit (SCP-011.2) impede leitura de `tenant_members`.

Nada disso participa hoje de qualquer transação Postgres. Toda a lógica é
avaliada em processo Node/Worker; não há isolamento contra corridas entre
resolver e mutation, porque não há mutation.

## 6. Alternativas arquiteturais

### 6.1 Estratégia A — SQL como nova autoridade canônica

Um resolver `SECURITY DEFINER` em Postgres passa a ser a autoridade única
(`resolve_commercial_seat_decision(_tenant uuid)` retornando
`(featureDecision, limitDecision)`). Runtime TypeScript delega ao resolver
SQL. Helpers TS permanecem apenas como tipos/validação (ou são
aposentados).

- Consistência: máxima (mesmo plano transacional que a mutation);
- Risco de dual resolver: eliminável se TS for delegador estrito;
- Complexidade: alta (portar catalog gate + precedência + billing map +
  reasons closed enum);
- Impacto no runtime atual: alto — reescrita da autoridade;
- Compatibilidade Supabase: total (Postgres nativo);
- Segurança: exige `search_path` fixo, `REVOKE ALL FROM PUBLIC/anon/authenticated`,
  `GRANT EXECUTE` restrito, identifiers qualificados, zero SQL dinâmico;
- Testabilidade: pgTAP + suíte de paridade cross-linguagem;
- Rollback: reversível (drop function); mas o runtime que passa a delegar
  fica indisponível sem ela;
- Operabilidade: mudanças de regra passam a exigir migration.

### 6.2 Estratégia B — Persistência de snapshot autoritativo

A decisão comercial é materializada previamente em uma tabela autoritativa
(ex.: `tenant_commercial_state` com `seat_limit`, `feature_gates`,
`source`, `reason`, `effective_at`, `version`) atualizada por processo
determinístico (webhook billing / recomputação agendada / trigger sobre
`tenant_entitlements` etc.). Enforcement lê a mesma linha `FOR UPDATE`.

- Consistência: alta se e somente se a materialização não fica stale;
- Risco de dual resolver: alto (TS ainda decide, materialização copia);
- Complexidade: alta (invalidação, versionamento, consistência eventual);
- Impacto: cria nova tabela autoritativa e novo caminho de escrita;
- Compatibilidade Supabase: total;
- Segurança: RLS + grants na nova tabela; escrita restrita ao processo de
  materialização;
- Testabilidade: paridade + testes de staleness;
- Rollback: mais complexo (tabela e produtor precisam sair juntos);
- Operabilidade: exige monitoração de defasagem.

### 6.3 Estratégia C — Conexão Postgres transacional server-side

Server functions abrem uma conexão direta (bypass PostgREST), fazem
`BEGIN`, executam o resolver comercial **e** a mutation de `tenant_members`
na mesma transação. O resolver comercial pode ainda ser SQL (A) ou uma
função server-side que roda dentro da transação e emite as queries via a
mesma conexão.

- Consistência: alta (mesma transação);
- Risco de dual resolver: médio se o resolver TS continuar autoritativo;
  eliminável se combinada com A;
- Complexidade: alta (gestão de pool, conexão dedicada, timeouts, retry);
- Impacto: introduz um caminho de acesso paralelo ao PostgREST;
- Compatibilidade Supabase: possível via connection string direta, mas foge
  do modelo padrão do projeto;
- Segurança: exige credenciais separadas + isolamento estrito;
- Testabilidade: exige Postgres real em CI para concorrência;
- Rollback: reversível;
- Operabilidade: adiciona superfície operacional (pool, health, limits).

## 7. Estratégia recomendada

**Estratégia A — SQL/Postgres como autoridade comercial canônica única**,
exposta somente por primitive RPC server-side restrita. A Estratégia C
(conexão Postgres direta / transaction callback multi-statement em
TypeScript) **não é selecionada**.

Modelo confirmado:

- PostgREST / RPC permanece como superfície de chamada;
- a transação ocorre **integralmente dentro da função Postgres**
  (`SECURITY DEFINER`), invocada pela server function autorizada como
  uma única chamada RPC;
- não existe transação multi-statement controlada pelo TypeScript;
- não existe pool ou conexão Postgres direta adicional;
- a Estratégia B (snapshot materializado) permanece rejeitada porque
  introduz staleness e dual authority por construção;
- a Estratégia C permanece registrada em §6.3 apenas como alternativa
  analisada — não é adotada em nenhuma subetapa futura.

Na SCP-012 final, a server function autorizada de mutation de membership
invoca uma **única RPC** (`SECURITY DEFINER`) que, no mesmo plano
transacional Postgres, adquire o lock (§10), executa o resolver
comercial SQL e aplica a mutation.

## 8. Plano de autoridade única

Para provar autoridade única (sem dual resolver e sem período de dual
authority persistido em produção):

1. o runtime TS de decisão comercial passa a **delegar** integralmente
   a `resolve_commercial_seat_decision(_tenant)` no mesmo conjunto de
   mudanças que materializa a função SQL (SCP-012.0.2 — cutover
   atômico);
2. os helpers puros em `feature-catalog.ts`, `feature-gate.ts`,
   `read-models.ts`, `limit-decision.ts` permanecem apenas como:
   - contrato de tipos (`CommercialFeatureDecision`,
     `CommercialLimitDecision`, enums de reason/source);
   - normalização de input estrito;
   - projeção DTO whitelisted;
3. após o merge da SCP-012.0.2, o caminho decisório TypeScript
   independente é **removido ou desativado** — ele não permanece como
   runtime alternativo, fallback, ou precedência comercial
   secundária;
4. o resolver TypeScript legado só é utilizado como oracle temporário
   dentro da suíte de paridade **durante a execução** da SCP-012.0.2, e
   não sobrevive ao cutover;
5. em produção, existe uma única autoridade comercial runtime a partir
   do merge da SCP-012.0.2.

## 9. Membership Mutation Boundary

Boundary server-side futuro (não implementado nesta etapa). O domínio
atual confirma:

- não existe tabela separada de invitations;
- `invited` é apenas um status de `public.tenant_members`;
- `user_id` é obrigatório (FK NOT NULL para `auth.users`);
- não existe runtime de convite ou aceite;
- o único INSERT existente é seed histórico em migration.

A futura **SCP-012.0.3** deverá primeiro definir quais operações são
realmente necessárias ao produto antes de qualquer materialização. A
etapa não deverá implementar automaticamente todas as operações
listadas anteriormente.

Candidatas (operações compatíveis com o domínio atual):

- `createMembership` para usuário `auth.users` já existente;
- `changeMembershipStatus` (transições dentro de `membership_status`);
- `changeMembershipRole` (dentro de `tenant_role`);
- `suspendMembership`;
- `revokeMembership`;
- `reactivateMembership`.

Dependentes de análise adicional (não assumidas):

- `invite` por e-mail;
- aceite de convite;
- `delete` físico da linha de membership;
- criação de `auth user` como parte da mutation.

Input permitido em cada mutation candidata:

- `targetUserId`;
- `targetStatus` (para transições);
- `targetRole` (para mudança de role).

Input **proibido** (nunca aceito do client):

- `tenantId`;
- `limit`, `used`, `remaining`, `source`;
- qualquer `CommercialLimitDecision` ou `CommercialFeatureDecision`;
- `billingStatus`;
- `seatDelta`;
- `featureKey`.

O `tenantId` vem exclusivamente de `requireTenant` (server-side).

## 10. Locking e transação

Comparação:

| Estratégia | Granularidade | Deadlocks | Cross-tenant | Retry | Observabilidade |
|---|---|---|---|---|---|
| `SELECT FOR UPDATE` em `public.tenants` (linha do tenant alvo) | por tenant | baixo se ordem consistente | isolado | simples | boa (linha real) |
| `pg_advisory_xact_lock(hashtext(tenant_id::text))` | por tenant (virtual) | baixo | isolado | simples | limitada (sem linha) |
| Tabela dedicada de lock/versionamento | por tenant | baixo | isolado | simples | ótima (versão explícita) |

**Recomendação**: `SELECT FOR UPDATE` sobre `public.tenants` pelo
`tenant_id` da requisição, no início da RPC, **antes** do resolver
comercial SQL. Motivos:

- ordem canônica única (sempre `tenants` primeiro) elimina deadlocks
  cross-mutation;
- serializa mutations concorrentes do mesmo tenant sem afetar outros;
- não requer nova tabela;
- fácil de observar via `pg_locks`.

## 11. Segurança

Para a primitive SQL futura:

- `SECURITY DEFINER` necessário para bypassar RLS de forma controlada
  dentro da mutation autorizada;
- `SET search_path = public, pg_temp` explícito;
- identifiers sempre qualificados (`public.tenant_members`, etc.);
- `REVOKE ALL ON FUNCTION … FROM PUBLIC, anon, authenticated`;
- `GRANT EXECUTE ON FUNCTION … TO service_role` (nunca `authenticated`
  direto);
- sem SQL dinâmico (`EXECUTE format(...)`);
- não retorna linhas internas — apenas o DTO whitelisted equivalente ao
  contrato canônico (§12);
- validação estrita de input (`IS NOT NULL` sobre `_tenant`);
- proibição de chamada direta do frontend garantida pelo
  `REVOKE`/`GRANT` acima.

## 12. Contrato canônico e matriz de paridade

### 12.1 Contrato canônico

A autoridade única (TS legada e SQL candidata durante a paridade)
utiliza **exclusivamente** o DTO abaixo:

```ts
type CommercialLimitDecision = {
  tenantId: string;
  featureKey: string;
  allowed: boolean;
  reason:
    | "entitled"
    | "not_entitled"
    | "limit_reached"
    | "billing_unknown"
    | "billing_attention_required"
    | "billing_blocked"
    | "not_evaluated";
  source: "tenant" | "plan" | "default" | "none";
  limit: number | null;
  used: number | null;
  requestedIncrement: number;
  remaining: number | null;
};
```

Valores **proibidos** no contrato, matrizes e planejamento (não são
razões nem sources válidos e nunca podem aparecer no runtime):

- `feature_not_cataloged` — valor proibido;
- `limit_unavailable` — valor proibido;
- `limit_invalid` — valor proibido;
- `within_limit` — valor proibido;
- `allowed` como `reason` — valor proibido;
- `tenant_entitlement` — valor proibido;
- `plan_entitlement` — valor proibido;
- `override` como `source` — valor proibido;
- `system` como `source` — valor proibido;
- `unknown` como `source` — valor proibido.

### 12.2 Matriz de paridade final

Matriz alinhada ao runtime aprovado (SCP-011.1 / SCP-011.2). `source do
CommercialFeatureDecision` denota a `source` já emitida pelo helper
`decideCommercialFeature` para o item de entitlement (`tenant`, `plan`,
`default`, `none`).

| Cenário | allowed | reason | source | limit | used | remaining |
|---|---|---|---|---|---|---|
| **§7.1** Catalog gate negativo | false | `not_evaluated` | `none` | null | null | null |
| **§7.2** Feature negativa (`not_entitled`) | false | `not_entitled` | source do CommercialFeatureDecision | null | null | null |
| **§7.2** Feature negativa (`billing_unknown`) | false | `billing_unknown` | source do CommercialFeatureDecision | null | null | null |
| **§7.2** Feature negativa (`billing_attention_required`) | false | `billing_attention_required` | source do CommercialFeatureDecision | null | null | null |
| **§7.2** Feature negativa (`billing_blocked`) | false | `billing_blocked` | source do CommercialFeatureDecision | null | null | null |
| **§7.2** Feature negativa (`not_evaluated`) | false | `not_evaluated` | source do CommercialFeatureDecision | null | null | null |
| **§7.3** Feature positiva + limite ausente/inválido | false | `not_evaluated` | `none` | null | null | null |
| **§7.4** Feature positiva + limite válido + uso indisponível | false | `not_evaluated` | source do limite | limite resolvido | null | null |
| **§7.5** Feature positiva + `used + requestedIncrement <= limit` | true | `entitled` | source do limite | limite resolvido | uso resolvido | `max(limit - used, 0)` |
| **§7.6** Feature positiva + `used + requestedIncrement > limit` | false | `limit_reached` | source do limite | limite resolvido | uso resolvido | 0 |
| **§7.7** `limit = 0`, `used = 0`, `requestedIncrement = 1` | false | `limit_reached` | source do limite | 0 | 0 | 0 |

### 12.3 Regras invariantes

- catalog gate negativo **nunca** carrega contexto comercial e **nunca**
  consulta `tenant_members`;
- **toda** razão comercial negativa (`not_entitled`, `billing_unknown`,
  `billing_attention_required`, `billing_blocked`, `not_evaluated`)
  produz **short-circuit antes da avaliação quantitativa**: nenhuma
  leitura de `tenant_members`, `limit`/`used`/`remaining` = `null`, e a
  `reason`/`source` do `CommercialFeatureDecision` é propagada
  exatamente como emitida;
- especificamente, `billing_attention_required` **não** dispara
  avaliação quantitativa normal — comportamento anterior de "avaliado
  normalmente" está revogado;
- feature positiva com limite ausente / inválido / não efetivo emite
  `not_evaluated` / `source = none` sem leitura de `tenant_members`;
- feature positiva com limite válido e leitura de uso indisponível
  emite `not_evaluated` preservando `source do limite`;
- `remaining` só é numérico quando existe `limit` e `used`; caso
  contrário é `null`;
- precedência de source: `tenant > plan > default > none`, preservada
  entre autoridade TS legada e SQL candidata durante a paridade.

## 13. Estratégia de testes

- **unitários TS**: continuam verdes contra o contrato canônico;
- **pgTAP / SQL unit**: cobrem cada branch do resolver SQL;
- **paridade cross-linguagem** (executada apenas durante a SCP-012.0.2):
  fixtures compartilhadas alimentam TS (oracle legado temporário) e SQL
  (candidata), comparando outputs byte-a-byte segundo §12.2;
- **integração**: server fn invocando resolver SQL sob `requireTenant`;
- **boundary**: cada mutation de membership recusa `tenantId`, `limit`,
  `seatDelta`, `featureKey` no input;
- **autorização**: matriz roles × operações;
- **RLS/grants**: `authenticated` não pode `INSERT/UPDATE/DELETE`
  diretamente em `tenant_members`; a RPC não é `EXECUTE` para `anon` /
  `authenticated`;
- **rollback**: drop da RPC → runtime cai para read-only aprovado;
- **idempotência**: repetir mutation não altera `used`;
- **concorrência real (Postgres real, clientes concorrentes reais,
  lock por tenant, rollback integral, isolamento cross-tenant)**:
  - **Cenário A — sem capacidade remanescente**: `limit = used`,
    `remaining = 0`, `requestedIncrement = 1`, duas ou mais mutations
    concorrentes; zero mutations passam; todas retornam
    `reason = limit_reached`.
  - **Cenário B — uma unidade remanescente**: `limit − used = 1`,
    `requestedIncrement = 1`, duas mutations concorrentes; exatamente
    uma mutation passa; a outra retorna `reason = limit_reached`.
  - **Cenário C — capacidade remanescente k**: `limit − used = k`,
    `requestedIncrement = 1`, `N` mutations concorrentes com `N > k`;
    exatamente `k` mutations passam; as demais retornam
    `reason = limit_reached`.
- **cross-tenant**: mutation de tenant A não bloqueia tenant B;
- **regressão SCP-011**: todos os specs da cadeia SCP-011 continuam
  verdes sem mudança;
- **client sem autoridade**: cliente não decide `allowed`, `reason`,
  `remaining` ou `limit`; qualquer tentativa é rejeitada pelo boundary.

Ambiente: Postgres real (não mock) é obrigatório para concorrência.

## 14. Sequenciamento

Sequência futura consolidada, sem período persistido de dual resolver
em produção:

1. **SCP-012.0** — Impact Analysis (esta etapa).
2. **SCP-012.0.1** — Canonical Decision Contract, Atomic Cutover
   Sequencing & Roadmap Cleanup (correção documental; nenhuma
   implementação).
3. **SCP-012.0.2** — Transaction-Safe Commercial Authority
   Materialization & Atomic Runtime Cutover — em um único conjunto de
   mudanças auditável: materialização do resolver SQL; suíte de
   paridade; delegação do runtime ao SQL; remoção/desativação do
   caminho decisório TypeScript independente; prova de que não existe
   período persistido de dual authority.
4. **SCP-012.0.3** — Membership Mutation Boundary Planning &
   Materialization (definição prévia das operações realmente
   necessárias ao produto conforme §9; boundary server-side sem
   enforcement ainda).
5. **SCP-012** — Atomic Enforcement Integration (lock em
   `public.tenants`, resolver SQL, delta e mutation na mesma RPC /
   transação Postgres).

Regra explícita: **não existe subetapa** que materialize o resolver SQL
sem cutover do runtime na mesma etapa. A SCP-012.0.2 é atômica.

## 15. Roadmap

Bloco canônico aplicado em
`docs/architecture/ROADMAP_ARCHITECTURAL.md`:

```
16. SCP-012 — Commercial Seat Limit Atomic Enforcement Integration — Blocked: architectural prerequisites required.
16.0 SCP-012.0 — Transaction-Safe Commercial Authority & Membership Mutation Boundary Impact Analysis — Accepted.
16.0.1 SCP-012.0.1 — Canonical Decision Contract, Atomic Cutover Sequencing & Roadmap Cleanup — Accepted.
16.0.1.1 SCP-012.0.1.1 — Deterministic Full-Section Rewrite, Evidence Lock & Git Readiness — Accepted.
16.0.1.2 SCP-012.0.1.2 — Canonical Concurrency, Internal Roadmap & Accepted Status Finalization — Ready for External Audit.
16.0.2 SCP-012.0.2 — Transaction-Safe Commercial Authority Materialization & Atomic Runtime Cutover — futura; não iniciada.
```




## 16. Hard Gates

- nenhuma implementação antes da aprovação da SCP-012.0;
- nenhum resolver SQL criado nesta etapa;
- nenhuma mutation criada;
- nenhum enforcement criado;
- nenhuma mudança em RLS ou grants;
- nenhuma autoridade duplicada planejada — a transição é atômica em
  SCP-012.0.2;
- SCP-012 permanece bloqueada.

## 17. Confirmações negativas

- nenhum código de produção alterado;
- nenhum teste alterado;
- nenhuma migration criada;
- nenhum schema alterado;
- nenhuma RLS policy alterada;
- nenhum grant criado;
- nenhuma mutation criada;
- nenhum trigger criado;
- nenhum lock criado;
- nenhum enforcement implementado;
- nenhum provider integrado;
- nenhum frontend alterado.
